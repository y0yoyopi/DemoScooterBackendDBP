package com.dpndcs4demodsbdbp.dpndcs4demodsbdbp.user_locationsNONECESARIO.domain;

/*
import jakarta.persistence.Column;
import jakarta.persistence.Embeddable;
import lombok.Getter;
import lombok.Setter;

import java.io.Serializable;
import java.util.Objects;

@Embeddable
@Getter
@Setter
public class TenantCoordinateId implements Serializable {

    @Column(name = "tenant_id")
    private Long tenantId;

    @Column(name = "coordinate_id")
    private Long coordinateId;

    public TenantCoordinateId() {}

    public TenantCoordinateId(Long tenantId, Long coordinateId) {
        this.tenantId = tenantId;
        this.coordinateId = coordinateId;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        TenantCoordinateId that = (TenantCoordinateId) o;

        return Objects.equals(tenantId, that.tenantId) &&
                Objects.equals(coordinateId, that.coordinateId);
    }

    @Override
    public int hashCode() {
        return Objects.hash(tenantId, coordinateId);
    }
}*/
